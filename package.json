{
  "name": "sms-service",
  "version": "1.0.0",
  "description": "A service that sends communicates to users via SMS and other supported route",
  "main": "src/index.js",
  "scripts": {
    "build": "npm install && tsc",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "ts-node src/index.ts",
    "start:dev": "nodemon"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/amqplib": "^0.8.2",
    "@types/axios": "^0.14.0",
    "@types/dotenv": "^8.2.0",
    "@types/node": "^20.11.7",
    "rimraf": "^5.0.5",
    "ts-node": "^10.5.0",
    "typescript": "^4.5.5"
  },
  "dependencies": {
    "amqplib": "^0.8.0",
    "axios": "^0.27.2",
    "dotenv": "^16.4.1"
  }
}
